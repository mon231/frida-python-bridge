{
  "name": "frida-python-bridge-example",
  "private": true,
  "type": "module",
  "scripts": {
    "build:frida-compile": "frida-compile index.ts -o agent.js -c",
    "watch:frida-compile": "frida-compile index.ts -o agent.js -w",
    "build:esbuild": "esbuild --bundle --format=esm --outfile=agent.js index.ts"
  },
  "dependencies": {
    "frida-python-bridge": "file:.."
  },
  "devDependencies": {
    "@types/frida-gum": "^19.0.0",
    "esbuild": "^0.25.0",
    "frida-compile": "^19.0.0"
  }
}
